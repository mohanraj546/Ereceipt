<resources>

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        &lt;!&ndash; Customize your theme here. &ndash;&gt;
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>-->

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowActionModeOverlay">true</item>
<!--
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentNavigation">false</item>
-->
        <!-- Customize your theme here. -->

        <!-- This disables the black preview screen -->
        <item name="android:windowDisablePreview">true</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:background">@color/splash_background</item>
    </style>

    <style name="helvetica_regular_font" parent="AppTheme">
        <item name="fontFamily">@string/helvetica_font</item>
        <item name="fontFamilyType">@string/helvetica_font_family</item>
    </style>

    <style name="helvetica_bold_font" parent="AppTheme">
        <item name="fontFamily">@string/helveticabold_font</item>
        <item name="fontFamilyType">@string/helvetica_font_family</item>
    </style>

    <style name="CustomDialog" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@drawable/border_acct_reg</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="MyMaterialTheme" parent="MyMaterialTheme.Base">

    </style>

    <style name="MyMaterialTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/pay_rcv_tool_bar_bg</item>
        <item name="colorPrimaryDark">@color/pay_rcv_tool_bar_bg</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
